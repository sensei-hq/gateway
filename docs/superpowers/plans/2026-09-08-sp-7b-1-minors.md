# SP-7b.1 — minors pass Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking. Task ids in this file
> are `SP-7b.1 Task N` and are NOT the parent slice's `SP-7b Task 1–8`, which shipped in PR #54.

**Goal:** Close the minors the SP-7b whole-slice review left open — two correctness/test-integrity
items, one unbuilt operator surface, and two documents that state things the code does not do.

**Why this is a plan and not a spec→plan cycle:** every item is a *recorded finding awaiting
implementation*, not open design. Each was named by the SP-7b whole-slice review or found while
verifying its record; where a genuine fork existed the call is made in the task and the reasoning
recorded.

**Why this file exists at all:** the parent plan numbers its work `Task 1–8` and the spec numbers
its criteria `AC1–AC12`. A follow-on pass with no plan of its own gets an invented, colliding
sequence, which is exactly what happened before this file was written — the same convention
`SP-DATA-4.1` established for the same reason.

**Tech Stack:** Rust 2024, existing `orchestrator`/`orchestrator-core`/`torii` crates.

**Baseline that must not regress:** `cargo test --workspace` = **1755 passed / 0 failed** at
`24d1868` (`main`, PR #54 — 1754 before the flake guard); `cargo fmt --all --check` and
`cargo clippy --workspace --all-targets -- -D warnings` both exit 0. Now **1760 / 0**: Tasks 1–4
added five tests (2 + 1 + 2).

---

## The five items, and why each is here

| # | Item | Class | Source | Status |
|---|---|---|---|---|
| 1 | A duplicated tool name dropped every copy of the schema | correctness | SP-7b review | ✅ `4901bef` |
| 2 | `dropped_deps` could be hard-wired to `0` with the suite green | test integrity | SP-7b review | ✅ `3f88cb7` |
| 3 | `torii` had no `ContextBudgeted` arm — the operator surface was unbuilt | unbuilt surface | SP-7b review, spec §5.4 | ✅ `194da8c` |
| 4 | Spec §5.2 cited retracted rules | false spec claim | SP-7b review | ✅ `4a45f2c` |
| 5 | Blackboard design D5 is false in code, in both halves | false spec claim | found verifying §9 | ✅ `c205b4b` |

Not in this pass, deliberately: a budgeted node being effectively **single-turn** is already
explicit in spec §2 and pinned by
`a_budgeted_agent_that_calls_a_tool_busts_the_window_on_the_next_turn` — a recorded limitation, not
a defect. The review counted **seven** minors; five were written down and are the five above. The
other two were never recorded durably and are unrecoverable; the list is closed at five.

**Sequenced before this pass, not part of it:** the SP-DATA-5 clamp-signal flake (`c177a72`) — a
poisoned `tracing` callsite, fixed before PR #54 so the branch was not handed to review with a
red-capable suite. It belongs to neither slice's task numbering.

---

## Task 1: A duplicated tool name dropped every copy of the schema — ✅ `4901bef`

- [x] Red: `a_duplicated_tool_name_drops_only_the_planned_schema`, driving the real `plan_budget`.
- [x] `join_bounded` drops by POSITION (the last `dropped_tools.len()`), not by name match.
- [x] `debug_assert` states the tail invariant both plan producers establish.
- [x] Red: `a_multi_schema_drop_takes_the_tail_and_records_it_in_reverse` — no test in the file
      dropped more than one schema, so the reverse-order claim was unguarded and the assert's own
      ordering was unverified.
- [x] Mutation-verified: each mutation reddens exactly one of the two tests.

## Task 2: `dropped_deps` could be hard-wired to `0` — ✅ `3f88cb7`

- [x] Reproduce: `dropped_deps: 0` left the ENTIRE workspace green.
- [x] Red: `a_dropped_dependency_is_counted_in_the_journaled_budget_row`.
- [x] Fixture derivation recorded — a drop needs a HEADING wider than its share (equal keys and
      oversized bodies both cannot produce one), hence a 1200-character node id; all four premises
      asserted before the drive because three fail silently.
- [x] Mutation-verified both ways.

## Task 3: `torii` had no `ContextBudgeted` arm — ✅ `194da8c`

- [x] Red: one text and one `--json` test, the latter asserted structurally not on prose.
- [x] `run status` reports nodes, dropped dependency count, NAMED schemas, retained bytes, window.
- [x] Additivity preserved — the two existing `status_*_is_byte_identical` tests still pass.
- [x] Spec §8 records the surface.

## Task 4: Spec §5.2 cited retracted rules — ✅ `4a45f2c`

- [x] Step 1 named the human path's wrapper as the model path's renderer.
- [x] The "no shipped test covers a mixed-size set" claim was already closed by a 1.5 MB-vs-10-byte
      fixture.
- [x] §5.3 re-derived against the code rather than assumed stale — it HOLDS, left alone.

## Task 5: Blackboard design D5 is false in code — ✅ `c205b4b`

`docs/superpowers/specs/2026-08-10-sp1-blackboard-wiring-design.md`, three stale spots.

- [x] **D5 half 1 (lines 72–73) — the summary.** "Each dependency contributes its `summary` if
      present" describes a branch nothing can enter: `summary: Some` has ZERO producers in the
      workspace and `resolve_context` (`mod.rs:1695-96`) calls `ctx.load(&r)` unconditionally. The
      field does round-trip journal→fold→store (`support.rs:152-161`), so the seam exists — record
      that it is a seam, not a behaviour.
- [x] **D5 half 2 (lines 72–73) — the halt. The load-bearing half.** D5 defers summarization on the
      grounds that "the **existing** per-turn window budget (`PromptOverBudget`, a loud halt) covers
      overflow — never silent truncation". `PromptOverBudget` was DELETED by SP-7a (tombstone,
      `orchestrator-core/src/error.rs:75`) and the invariant was deliberately REVERSED by SP-7b.
      Both clauses of the deferral's justification are gone.
- [x] **§6 read path (line 120)** — "or use `ref.summary` when present per D5". Same dead branch.
- [x] **Invariant 7 (line 218)** — "Over-budget is loud … halts with `PromptOverBudget`, never
      silent truncation." Now false on both the mechanism and the outcome.
- [x] Amend rather than silently rewrite, per the convention SP-7b §5.5 uses ("an earlier revision
      of this cell said …"): a design record's history is worth keeping.
- [x] While there: SP-7b §4.5's citations have drifted — `agent.rs:549`/`781` are now `992`/`1220`.

**Not a code change.** Nothing in Task 5 alters behaviour; the dead field stays until something
populates it, which is a slice of its own.

---

## Verified after Tasks 1–5 — **the pass is COMPLETE**

`cargo test --workspace` **1760 passed / 0 failed, real exit 0** across 35 suites · `clippy
--all-targets -D warnings` 0 · `fmt --check` 0. Unchanged by Task 5, which touches no code.

## What comes after Task 5

SP-7c has no spec and nothing in the repo names it, so it begins at `/sensei:design`, not here. Two
verified findings belong in that conversation:

- **Multimodal window blind spot.** `Message::attachments` are not counted by the estimator at all
  (`gateway/src/engine/util.rs:126`) — a documented decision with a stated residual: a multimodal
  call is estimated on its text alone, so the gate can admit a candidate its images push over. The
  one correctness hole in the window story neither SP-7a nor SP-7b closed.
- **Compaction's precondition.** Spec §4.5's input drift is real and present: a re-fetched
  Observation changes the transcript by wall-clock. Compacting the transcript would build a
  determinism claim on a drifting input. `ContextStore::put` is also a loud `ContextKeyCollision`
  on re-write (`postgres.rs:379-393`), so a produce-time summary is one-shot.
