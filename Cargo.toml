[workspace]
members = ["crates/kernel", "crates/cloud-providers", "crates/gateway", "crates/gateway-embedded"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "thin"
strip = true
codegen-units = 1
